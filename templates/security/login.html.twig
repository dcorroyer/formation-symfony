{% extends 'base.html.twig' %}

{% block title %}
  Connexion
{% endblock title %}

{% block body %}
  <h1>Connexion</h1>

  {% if error %}
    <div class="alert alert-danger">
      {{ error.message }}
    </div>
  {% endif %}

  {{ form_start(formView) }}

  {{ form_widget(formView) }}

  <button class="btn btn-success mt-2">Connexion</button>

  {{ form_end(formView) }}
{% endblock body %}
